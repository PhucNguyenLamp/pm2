info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/users/login
  body:
    type: json
    data: |-
      {
        "username": "phuc1",
        "password": "mustbeover8"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
